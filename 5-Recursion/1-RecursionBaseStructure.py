
# this is the base structure of any resursive function
def recursiceFunction(Some_Parameters):
    """ 
    try writing the recursive procedure first then the termination condition
    """
    if (baseCondition):    # this is where we terminate the recursive function
        """
        some code to terminate further recursion
        """
    else:       # this is where we make the recursion call
        """
        Sime code to carry on the recursion
        """